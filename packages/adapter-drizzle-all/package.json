{
  "name": "@next-auth/drizzle-adapter-all",
  "version": "0.0.1",
  "description": "Drizzle adapter for next-auth.",
  "homepage": "https://authjs.dev",
  "repository": "https://github.com/nextauthjs/next-auth",
  "bugs": {
    "url": "https://github.com/nextauthjs/next-auth/issues"
  },
  "author": "Anthony Shew",
  "main": "dist/index.js",
  "license": "ISC",
  "keywords": [
    "next-auth",
    "next.js",
    "oauth",
    "drizzle"
  ],
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf ./drizzle || echo 'Drizzle directory deleted'",
    "generate-schema": "drizzle-kit generate:mysql --schema=src/schema.ts --breakpoints",
    "test:init": "pnpm clean && pnpm generate-schema",
    "test": "pnpm test:init && jest",
    "build": "pnpm clean && drizzle-kit generate:sqlite --schema=src/schema.ts && tsc",
    "dev": "drizzle-kit generate:sqlite --schema=src/schema.ts && tsc -w"
  },
  "files": [
    "README.md",
    "dist"
  ],
  "peerDependencies": {
    "next-auth": "^4"
  },
  "dependencies": {
    "@next-auth/drizzle-adapter-sqlite": "workspace:*",
    "@next-auth/drizzle-adapter-mysql": "workspace:*",
    "@next-auth/drizzle-adapter-pg": "workspace:*",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@next-auth/adapter-test": "workspace:*",
    "@next-auth/tsconfig": "workspace:*",
    "@types/better-sqlite3": "^7.6.3",
    "@types/uuid": "^8.3.3",
    "better-sqlite3": "^8.3.0",
    "mysql2": "^3.2.0",
    "postgres": "^3.3.4",
    "drizzle-kit": "^0.17.6",
    "drizzle-orm": "^0.25.4",
    "jest": "^27.4.3",
    "next-auth": "workspace:*"
  },
  "jest": {
    "preset": "@next-auth/adapter-test/jest"
  }
}
