{
  "name": "@next-auth/drizzle-adapter-mysql",
  "version": "0.0.1",
  "description": "Drizzle adapter for next-auth.",
  "homepage": "https://authjs.dev",
  "repository": "https://github.com/nextauthjs/next-auth",
  "bugs": {
    "url": "https://github.com/nextauthjs/next-auth/issues"
  },
  "author": "Anthony Shew",
  "main": "dist/index.js",
  "license": "ISC",
  "keywords": [
    "next-auth",
    "next.js",
    "oauth",
    "drizzle"
  ],
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf ./drizzle || echo 'Drizzle directory deleted'",
    "generate-schema": "drizzle-kit generate:mysql --schema=src/schema.ts --breakpoints",
    "test:init": "pnpm clean && pnpm generate-schema",
    "test": "pnpm test:init && ./tests/test.sh",
    "build": "pnpm clean && drizzle-kit generate:mysql --schema=src/schema.ts && tsc",
    "dev": "drizzle-kit generate:mysql --schema=src/schema.ts && tsc -w"
  },
  "files": [
    "README.md",
    "dist"
  ],
  "peerDependencies": {
    "next-auth": "^4"
  },
  "devDependencies": {
    "@next-auth/adapter-test": "workspace:*",
    "@next-auth/tsconfig": "workspace:*",
    "mysql2": "^3.2.0",
    "drizzle-kit": "^0.17.6",
    "drizzle-orm": "^0.25.4",
    "jest": "^27.4.3",
    "next-auth": "workspace:*"
  },
  "jest": {
    "preset": "@next-auth/adapter-test/jest"
  }
}
